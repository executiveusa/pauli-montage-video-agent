# OpenMontage - Environment Variables
# Copy this to .env and fill in your keys

# --- Image + video gateway ---
FAL_KEY=                     # FLUX images, Google Veo video, Kling video, MiniMax video, Recraft images
                             # Get one at https://fal.ai/dashboard/keys

# --- Google (one key unlocks image gen + TTS) ---
GOOGLE_API_KEY=              # Google Imagen images, Google Cloud TTS (700+ voices, 50+ languages)
                             # Get one at https://aistudio.google.com/apikey

# --- Voice ---
ELEVENLABS_API_KEY=          # TTS narration, music generation, sound effects
OPENAI_API_KEY=              # OpenAI TTS fallback and DALL-E image generation
XAI_API_KEY=                 # Grok image generation/editing and Grok video generation
# Piper local voices do not require env vars; install `piper-tts` via pip

# --- Music ---
SUNO_API_KEY=                # Suno AI music generation (full songs, instrumentals, any genre)

# --- Video Generation ---
HEYGEN_API_KEY=              # HeyGen API (VEO, Sora, Runway, Kling, Seedance via single key)
RUNWAY_API_KEY=              # Runway Gen-4 (direct API, alternative to fal.ai routing)
VIDEO_GEN_LOCAL_ENABLED=     # Set to "true" for local video gen (needs GPU + diffusers)
VIDEO_GEN_LOCAL_MODEL=       # Local model: wan2.1-1.3b, wan2.1-14b, hunyuan-1.5, ltx2-local, cogvideo-5b
MODAL_LTX2_ENDPOINT_URL=    # Modal self-hosted LTX-2 endpoint (optional)

# --- Stock Media ---
PEXELS_API_KEY=              # Pexels stock footage/images (free)
PIXABAY_API_KEY=             # Pixabay stock footage/images (free)
UNSPLASH_ACCESS_KEY=         # Unsplash stock images (free developer key)

# --- Analysis ---
HF_TOKEN=                    # HuggingFace token — enables speaker diarization in transcriber

# --- Avatar (local installs) ---
# WAV2LIP_PATH=              # Path to cloned Wav2Lip repo (for lip sync)
# SADTALKER_PATH=            # Path to cloned SadTalker repo (for talking head avatars)

# --- Hosted multi-account Studio API ---
YAPPY_STUDIO_API_URL=http://127.0.0.1:8000
YAPPY_AUTH_MODE=hosted
# Required for signed API sessions. Use at least 32 random bytes.
YAPPY_AUTH_SIGNING_SECRET=
# file: single-node JSON persistence; postgres: normalized account tables + project RLS.
YAPPY_REPOSITORY_BACKEND=file
YAPPY_ACCOUNT_STORE_PATH=.yappy-clipz/accounts.json
# local stores owner-private files; s3 streams to an S3/R2-compatible bucket.
YAPPY_STORAGE_BACKEND=local
YAPPY_STORAGE_ROOT=.yappy-clipz/objects
YAPPY_STORAGE_BUCKET=
YAPPY_STORAGE_REGION=us-east-1
YAPPY_STORAGE_ENDPOINT_URL=
YAPPY_STORAGE_SIGNING_SECRET=
YAPPY_MAX_UPLOAD_BYTES=2147483648
# Recovery delivery: disabled, file (local owner-readable outbox), or smtp.
YAPPY_RECOVERY_DELIVERY=disabled
YAPPY_RECOVERY_OUTBOX_PATH=.yappy-clipz/recovery-outbox
YAPPY_RECOVERY_RESET_BASE_URL=http://localhost:3000
YAPPY_SMTP_HOST=
YAPPY_SMTP_PORT=587
YAPPY_SMTP_SENDER=
YAPPY_SMTP_USERNAME=
YAPPY_SMTP_PASSWORD=
YAPPY_SMTP_TLS=true

# --- OneDrive media source (Slice 1: read-only Files.Read) ---
# Register a Microsoft Entra web app that supports personal Microsoft accounts.
YAPPY_MICROSOFT_CLIENT_ID=
YAPPY_MICROSOFT_CLIENT_SECRET=
# Must exactly match the web-app redirect URI registered with Microsoft.
YAPPY_MICROSOFT_REDIRECT_URI=http://localhost:3000/api/sources/onedrive/callback
# "consumers" keeps this connector personal-Microsoft-account only; use "common" only if work/school accounts are deliberately supported later.
YAPPY_MICROSOFT_OAUTH_TENANT=consumers
# Separate owner-controlled secret used only to encrypt delegated source credentials and OAuth state. Minimum 32 bytes.
YAPPY_SOURCE_TOKEN_ENCRYPTION_SECRET=
# Optional single-node file store. PostgreSQL mode uses migrations/0003_external_source_connections.sql instead.
YAPPY_SOURCE_STORE_PATH=.yappy-clipz/sources.json
