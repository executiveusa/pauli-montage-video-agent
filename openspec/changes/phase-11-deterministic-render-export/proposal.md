# Phase 11 Proposal — Deterministic Render, Preview, Edit, and Export

Compile canonical Timeline v1 and Asset v1 state into immutable render manifests, execute through a no-shell FFmpeg worker, verify outputs with ffprobe, register canonical derivatives, and package delivery evidence. Remotion remains an optional license-gated plan adapter.
