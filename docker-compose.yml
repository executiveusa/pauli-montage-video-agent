services:
  api:
    build: .
    restart: unless-stopped
    environment:
      YAPPY_PROJECT_ROOT: /data/projects
      REDIS_URL: redis://redis:6379/0
    volumes:
      - project_data:/data/projects
      - render_output:/app/output
    ports:
      - "8000:8000"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  worker:
    build: .
    restart: unless-stopped
    command: ["python", "-m", "yappy_clipz.worker"]
    environment:
      YAPPY_PROJECT_ROOT: /data/projects
      REDIS_URL: redis://redis:6379/0
    volumes:
      - project_data:/data/projects
      - render_output:/app/output
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  project_data:
  render_output:
  redis_data:
