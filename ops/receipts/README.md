# Phase receipts

GRINIONS writes machine-readable completion receipts here after post-merge verification.

A receipt is evidence, not a substitute for Git/GitHub release truth. It should reference the baseline SHA, phase/OpenSpec ID, completed Beads, verification results, squash SHA, deployment evidence when applicable, rollback status, and next phase.
