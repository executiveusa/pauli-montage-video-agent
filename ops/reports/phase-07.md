# Phase 07 — Persistent Authenticated Studio API

Adds PostgreSQL persistence, signed owner sessions, scoped service tokens, principal-derived tenant identity, local/test compatibility, strict hosted CORS configuration, additive migrations, backup/restore boundaries, and exact-head verification. Production activation requires external database and secret configuration and remains fail-closed when absent.
